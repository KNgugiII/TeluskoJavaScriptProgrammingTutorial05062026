/* Prints one element of an array at a time thereby allowing further computations, for instance, multiplying each element by 2 etc */

let arrValues=[36,45,67,88,101]

/* To print each element of the array individually, we'll use the For Each method of the array */

    arrValues.forEach((n) => {
        console.log(n);
    });